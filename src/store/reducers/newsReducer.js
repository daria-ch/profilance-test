const initialState = {
    news: [],
    article: null,
    error: null
};

const newsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};


export default newsReducer;